name: ceres_wallet_core
description: "Dart FFI bindings for Trust Wallet Core — ETH, SOL, SUI, TRX and 27+ EVM L2 chains."
version: 0.1.6
homepage: https://github.com/SauceWu/ceres-wallet-core
repository: https://github.com/SauceWu/ceres-wallet-core
issue_tracker: https://github.com/SauceWu/ceres-wallet-core/issues
topics:
  - wallet
  - crypto
  - ffi
  - blockchain

environment:
  sdk: ^3.9.0
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  ffi: ^2.1.3
  plugin_platform_interface: ^2.0.2
  protobuf: ^3.1.0
  fixnum: ^1.1.0
  hooks: ^1.0.0
  code_assets: ^1.0.0

dev_dependencies:
  ffi: ^2.1.3
  ffigen: ^13.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
